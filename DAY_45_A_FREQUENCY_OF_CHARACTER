Q89: Count frequency of a given character in a string.

/*
Sample Test Cases:
Input 1:
programming
g
Output 1:
2

*/

#include <stdio.h>

int main() {
    char s[1000];
    char target;
    if (fgets(s, sizeof(s), stdin) == NULL) return 0;
    // Remove newline from input line if present:
    int len = 0;
    while (s[len] != '\0' && s[len] != '\n') len++;
    s[len] = '\0';

    scanf(" %c", &target);

    int count = 0;
    for (int i = 0; s[i] != '\0'; i++) {
        if (s[i] == target) {
            count++;
        }
    }
    printf("%d\n", count);
    return 0;
}
