Q88: Replace spaces with hyphens in a string.

/*
Sample Test Cases:
Input 1:
hello world
Output 1:
hello-world

*/

#include <stdio.h>

int main() {
    char s[1000];
    if (fgets(s, sizeof(s), stdin) == NULL) return 0;
    int i = 0;
    while (s[i] != '\0' && s[i] != '\n') {
        if (s[i] == ' ') {
            s[i] = '-';
        }
        i++;
    }
    printf("%s", s);
    return 0;
}
