Q92: Find the first repeating lowercase alphabet in a string.

/*
Sample Test Cases:
Input 1:
stress
Output 1:
s

*/

#include <stdio.h>
#include <string.h>

int main() {
    char s[1000];
    if (fgets(s, sizeof(s), stdin) == NULL) return 0;
    size_t len = strlen(s);
    if (len > 0 && s[len-1] == '\n') {
        s[len-1] = '\0';
        len--;
    }

    int count[26] = {0};
    for (size_t i = 0; i < len; i++) {
        char ch = s[i];
        if (ch >= 'a' && ch <= 'z') {
            int idx = ch - 'a';
            if (count[idx] >= 1) {
                printf("%c\n", ch);
                return 0;
            } else {
                count[idx]++;
            }
        }
        // If uppercase letters or non‐letters appear,
        // you can either ignore or handle differently per problem spec.
    }

    // If no repeating lowercase alphabet found:
    printf("No repeating lowercase character\n");
    return 0;
}
